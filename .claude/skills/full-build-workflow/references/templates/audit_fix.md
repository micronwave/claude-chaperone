# Audit Fix — Phase <N>

> Generated by `/build-audit` (or `/re-audit`). Consumed by `/execute`.

**Scope:** diff of Phase <N> (files changed in `/build`).

**Audited by:** Claude (primary) [+ <second-opinion tool> if available]

**Loop iteration:** 1 of max 3

---

## Findings

Each finding has:
- **Severity:** critical / high / medium / low
- **Category:** correctness / security / performance / style / test coverage
- **Source:** `CLAUDE`, `<tool>`, `BOTH`, or `CONFLICT`

### Finding 1

- **Severity:** <level>
- **Category:** <category>
- **Source:** <source>
- **File:** `path/to/file.py:line`
- **Issue:** <what's wrong>
- **Fix:** <one sentence describing the change>
- **Rationale:** <why>

Optional — include a Before/After block only when the change spans multiple lines and a prose sentence is ambiguous:

```python
# Before
<current code>

# After
<fixed code>
```

### Finding 2

<same structure>

---

## Conflicts

If `CONFLICT:` findings are present, these need user input before `/execute` can proceed.

### Conflict 1

- **File:** `<path>:<line>`
- **Claude says:** <position>
- **<tool> says:** <position>
- **User decision needed:** choose (a) Claude's take, (b) tool's take, (c) neither, pursue third option

---

## Summary

- Total findings: <N>
- Critical: <N>
- High: <N>
- Medium: <N>
- Low: <N>
- Conflicts: <N>

**Recommendation:** <proceed to /execute | resolve conflicts first | escalate — too many critical>
