# Plan: <project/feature name>

> Generated by `/plan` from `plan/meta.md`. Reviewed by `/plan-audit`. Approved by user on <date>.

## Intent

<one paragraph restating what we're building and why, from the resolved meta-prompt>

## Success criteria

- [ ] <observable outcome 1>
- [ ] <observable outcome 2>
- [ ] <observable outcome 3>

## Scope boundary

**In scope:** <bullet list>

**Out of scope:** <bullet list — things we explicitly are NOT doing>

## Dependencies & assumptions

- **External dependencies:** <libraries, APIs, services>
- **Environmental assumptions:** <runtime, OS, configuration>
- **Skipped concerns:** <what we're deferring and why>

## Phases

### Phase 1 — <phase name>

**Goal:** <one sentence>

**Declared file scope (also in `plan/phase_1_scope.json`):**
- `path/to/file_1.py` — <what changes>
- `path/to/file_2.py` — <what changes>

**Dependencies:** none (first phase) | phase N complete

**Tests (written first in `/build`):**
- `test_<thing>` — asserts <observable behavior>
- `test_<thing>` — asserts <observable behavior>

**Success criteria:**
- [ ] <measurable outcome>

**Rollback:** <git revert target> | <manual undo steps>

**Estimated size:** <approx net new/changed lines>

---

### Phase 2 — <phase name>

<same structure>

---

## Parallelizable phases

Phases that can run in parallel via separate worktrees (no dependency conflicts): <list or "none">

## Overall rollback plan

If we need to abandon the whole build: <target commit hash> reverts to pre-start state.

## Open risks

- <risk 1 + mitigation>
- <risk 2 + mitigation>

---

## Scope contract format

For each phase, `/split-phases` also writes a machine-readable scope contract at `plan/phase_<N>_scope.json`. The scope-drift hook reads that file — NOT this plan file. See `.claude/skills/full-build-workflow/references/templates/SCOPE_SCHEMA.md` for the format. The file lists in this plan are human-readable; the JSON is the source of truth for enforcement.
